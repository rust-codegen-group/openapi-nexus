# DO NOT EDIT - This file is automatically generated.
# Any manual changes will be overwritten on the next generation.
# To make changes, modify the source code and regenerate this file.

openapi: 3.1.0
info:
  title: Enum Representation API
  description: 'This API demonstrates all 4 kinds of enum representation types: Externally Tagged, Internally Tagged, Adjacently Tagged, and Untagged'
  contact:
    name: Adam Basfop Cavendish
    email: GetbetterABC@yeah.net
  license:
    name: MIT OR Apache-2.0
    identifier: MIT OR Apache-2.0
  version: 1.0.0
paths:
  /adjacently-tagged:
    post:
      tags:
      - enum-repr
      summary: Handle adjacently tagged enum
      operationId: handle_adjacently_tagged
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdjacentlyTaggedEnum'
        required: true
      responses:
        '200':
          description: Successful operation
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdjacentlyTaggedEnum'
        '400':
          description: Invalid input
  /externally-tagged:
    post:
      tags:
      - enum-repr
      summary: Handle externally tagged enum
      operationId: handle_externally_tagged
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ExternallyTaggedEnum'
        required: true
      responses:
        '200':
          description: Successful operation
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExternallyTaggedEnum'
        '400':
          description: Invalid input
  /internally-tagged:
    post:
      tags:
      - enum-repr
      summary: Handle internally tagged enum
      operationId: handle_internally_tagged
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InternallyTaggedEnum'
        required: true
      responses:
        '200':
          description: Successful operation
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InternallyTaggedEnum'
        '400':
          description: Invalid input
  /mixed:
    post:
      tags:
      - enum-repr
      summary: Handle mixed enum (unit variants SimpleA, SimpleB and tuple variants VariantA(VariantA), VariantB(VariantB))
      operationId: handle_mixed
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MixedEnum'
        required: true
      responses:
        '200':
          description: Successful operation
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MixedEnum'
        '400':
          description: Invalid input
  /untagged:
    post:
      tags:
      - enum-repr
      summary: Handle untagged enum
      operationId: handle_untagged
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UntaggedEnum'
        required: true
      responses:
        '200':
          description: Successful operation
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UntaggedEnum'
        '400':
          description: Invalid input
components:
  schemas:
    AdjacentlyTaggedEnum:
      oneOf:
      - type: object
        description: Variant A with string and integer fields
        required:
        - data
        - ty
        properties:
          data:
            $ref: '#/components/schemas/VariantA'
            description: Variant A with string and integer fields
          ty:
            type: string
            enum:
            - ADJACENTLY_TAGGED_ENUM_VARIANT_A
      - type: object
        description: Variant B with boolean and float fields
        required:
        - data
        - ty
        properties:
          data:
            $ref: '#/components/schemas/VariantB'
            description: Variant B with boolean and float fields
          ty:
            type: string
            enum:
            - ADJACENTLY_TAGGED_ENUM_VARIANT_B
      description: |-
        Adjacently tagged enum

        The tag and content are separate fields:
        `{"ty": "VariantA", "data": {"field1": "...", "field2": 123}}`
    ExternallyTaggedEnum:
      oneOf:
      - type: object
        description: Variant A with string and integer fields
        required:
        - EXTERNALLY_TAGGED_ENUM_VARIANT_A
        properties:
          EXTERNALLY_TAGGED_ENUM_VARIANT_A:
            $ref: '#/components/schemas/VariantA'
            description: Variant A with string and integer fields
      - type: object
        description: Variant B with boolean and float fields
        required:
        - EXTERNALLY_TAGGED_ENUM_VARIANT_B
        properties:
          EXTERNALLY_TAGGED_ENUM_VARIANT_B:
            $ref: '#/components/schemas/VariantB'
            description: Variant B with boolean and float fields
      description: |-
        Externally tagged enum (default Serde representation)

        This is the default representation where the variant name is the key
        and the content is the value: `{"VariantA": {"field1": "...", "field2": 123}}`
    InternallyTaggedEnum:
      oneOf:
      - allOf:
        - $ref: '#/components/schemas/VariantA'
          description: Variant A with string and integer fields
        - type: object
          required:
          - ty
          properties:
            ty:
              type: string
              enum:
              - INTERNALLY_TAGGED_ENUM_VARIANT_A
        description: Variant A with string and integer fields
      - allOf:
        - $ref: '#/components/schemas/VariantB'
          description: Variant B with boolean and float fields
        - type: object
          required:
          - ty
          properties:
            ty:
              type: string
              enum:
              - INTERNALLY_TAGGED_ENUM_VARIANT_B
        description: Variant B with boolean and float fields
      description: |-
        Internally tagged enum

        The tag is a field inside the object alongside other fields:
        `{"ty": "VariantA", "field1": "...", "field2": 123}`
    MixedEnum:
      oneOf:
      - type: string
        description: Unit variant SimpleA
        enum:
        - SimpleA
      - type: object
        description: Tuple variant with VariantA payload
        required:
        - VariantA
        properties:
          VariantA:
            $ref: '#/components/schemas/VariantA'
            description: Tuple variant with VariantA payload
      - type: string
        description: Unit variant SimpleB
        enum:
        - SimpleB
      - type: object
        description: Tuple variant with VariantB payload
        required:
        - VariantB
        properties:
          VariantB:
            $ref: '#/components/schemas/VariantB'
            description: Tuple variant with VariantB payload
      description: |-
        Mixed enum with unit variants (SimpleA, SimpleB) and tuple variants (VariantA(VariantA), VariantB(VariantB))

        Serializes as externally tagged: `{"SimpleA": null}`, `{"VariantA": {"field1": "...", "field2": 123}}`,
        `{"SimpleB": null}`, `{"VariantB": {"field3": true, "field4": 1.23}}`
    UntaggedEnum:
      oneOf:
      - $ref: '#/components/schemas/VariantA'
        description: Variant A with string and integer fields
      - $ref: '#/components/schemas/VariantB'
        description: Variant B with boolean and float fields
      description: |-
        Untagged enum

        No tag is used, variants are distinguished by their structure:
        `{"field1": "...", "field2": 123}` or `{"field3": true, "field4": 1.23}`
    VariantA:
      type: object
      description: Variant A with string and integer fields
      required:
      - field1
      - field2
      properties:
        field1:
          type: string
          description: First field of variant A
        field2:
          type: integer
          format: int32
          description: Second field of variant A
    VariantB:
      type: object
      description: Variant B with boolean and float fields
      required:
      - field3
      - field4
      properties:
        field3:
          type: boolean
          description: First field of variant B
        field4:
          type: number
          format: double
          description: Second field of variant B
tags:
- name: enum-repr
  description: Enum representation examples
