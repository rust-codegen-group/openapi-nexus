openapi: 3.1.0
info:
  title: Union Type with Primitives Test
  description: Test fixture for union types with primitive members
  version: 1.0.0

paths:
  /test:
    post:
      summary: Test endpoint with union type
      operationId: test_union_primitives
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Foo'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Foo'

components:
  schemas:
    Foo:
      anyOf:
        - type: string
        - type: number
        - type: boolean
        - type: 'null'

