# DO NOT EDIT - This file is automatically generated.
# Any manual changes will be overwritten on the next generation.
# To make changes, modify the source code and regenerate this file.

openapi: 3.1.0
info:
  title: Additional Properties API
  description: API demonstrating OpenAPI additionalProperties with multiple levels of structs (RootLevel -> MiddleLevel -> LeafValue), each with HashMap fields.
  contact:
    name: Adam Basfop Cavendish
    email: GetbetterABC@yeah.net
  license:
    name: MIT OR Apache-2.0
    identifier: MIT OR Apache-2.0
  version: 1.0.0
paths:
  /leaf:
    post:
      tags:
      - additional-properties
      summary: Echo leaf payload (attributes map).
      operationId: post_leaf
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LeafValue'
        required: true
      responses:
        '200':
          description: Echo leaf
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeafValue'
        '400':
          description: Invalid input
  /middle:
    post:
      tags:
      - additional-properties
      summary: Echo middle-level payload (nested + extra maps).
      operationId: post_middle
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MiddleLevel'
        required: true
      responses:
        '200':
          description: Echo middle level
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MiddleLevel'
        '400':
          description: Invalid input
  /root:
    post:
      tags:
      - additional-properties
      summary: Echo root payload (all three levels with additional-properties-style maps).
      operationId: post_root
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RootLevel'
        required: true
      responses:
        '200':
          description: Echo root with children
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RootLevel'
        '400':
          description: Invalid input
components:
  schemas:
    LeafValue:
      type: object
      description: 'Leaf level: fixed fields plus maps with string and integer value kinds.'
      required:
      - value
      - attributes
      properties:
        attributes:
          type: object
          description: 'Additional string attributes (additionalProperties: string).'
          additionalProperties:
            type: string
          propertyNames:
            type: string
        extras:
          type: object
          description: 'Free-form additional properties (additionalProperties: true).'
          additionalProperties: true
        label:
          type:
          - string
          - 'null'
          description: Optional label.
        scores:
          type: object
          description: 'Scores by name (additionalProperties: integer).'
          additionalProperties:
            type: integer
            format: int32
          propertyNames:
            type: string
        value:
          type: integer
          format: int32
          description: Numeric value.
    MiddleLevel:
      type: object
      description: 'Middle level: fixed fields plus maps with object ref and boolean value kinds.'
      required:
      - key
      - count
      - nested
      properties:
        count:
          type: integer
          format: int32
          description: Count.
        extras:
          type: object
          description: 'Free-form additional properties (additionalProperties: true).'
          additionalProperties: true
        flags:
          type: object
          description: 'Flags by name (additionalProperties: boolean).'
          additionalProperties:
            type: boolean
          propertyNames:
            type: string
        key:
          type: string
          description: Key identifying this middle node.
        nested:
          type: object
          description: 'Nested leaf objects by name (additionalProperties: LeafValue).'
          additionalProperties:
            $ref: '#/components/schemas/LeafValue'
          propertyNames:
            type: string
    RootLevel:
      type: object
      description: 'Root level: fixed fields plus maps with object ref and array value kinds.'
      required:
      - id
      - name
      - children
      properties:
        children:
          type: object
          description: 'Child nodes by name (additionalProperties: MiddleLevel).'
          additionalProperties:
            $ref: '#/components/schemas/MiddleLevel'
          propertyNames:
            type: string
        extras:
          type: object
          description: 'Free-form additional properties (additionalProperties: true).'
          additionalProperties: true
        id:
          type: integer
          format: int64
          description: Root id.
        labels:
          type: object
          description: 'Labels by key (additionalProperties: array of string).'
          additionalProperties:
            type: array
            items:
              type: string
          propertyNames:
            type: string
        name:
          type: string
          description: Root name.
tags:
- name: additional-properties
  description: Multi-level structs with HashMap (additionalProperties)
