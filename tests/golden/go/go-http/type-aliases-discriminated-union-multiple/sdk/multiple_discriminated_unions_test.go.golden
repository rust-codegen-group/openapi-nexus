// Code generated by openapi-nexus. DO NOT EDIT.
// Multiple Discriminated Unions Test
//
// Test fixture with multiple discriminated unions to verify Kind types
// are properly namespaced and don't conflict.
// This reproduces the original bug where both unions would generate
// the same Kind.ts file causing conflicts.
// 
//
// Generated from OpenAPI doc version 1.0.0

package sdk

import (
	"net/http"

	"example.com/sdk/internal/config"
	"example.com/sdk/internal/hooks"
	"example.com/sdk/apis"
)

// MultipleDiscriminatedUnionsTestSDK is the main SDK struct
type MultipleDiscriminatedUnionsTestSDK struct {
	sdkConfiguration config.SDKConfiguration
	hooks            *hooks.Hooks
	default_ *apis.Default
}

// Ensure MultipleDiscriminatedUnionsTestSDK implements hooks.SDK interface
var _ hooks.SDK = (*MultipleDiscriminatedUnionsTestSDK)(nil)

// New creates a new instance of MultipleDiscriminatedUnionsTestSDK
func New(opts ...SdkOption) (*MultipleDiscriminatedUnionsTestSDK, error) {
	sdkConfig := config.NewSDKConfiguration()
	h := hooks.New()

	// Apply options
	for _, opt := range opts {
		if err := opt(&sdkConfig); err != nil {
			return nil, err
		}
	}

	// Initialize hooks
	sdkConfig = h.SDKInit(sdkConfig)

	sdk := &MultipleDiscriminatedUnionsTestSDK{
		sdkConfiguration: sdkConfig,
		hooks:            h,
	}

	// Initialize sub-clients
	sdk.default_ = apis.NewDefault(sdk, sdkConfig, h)

	return sdk, nil
}

// SdkOption is a function that configures the SDK
type SdkOption func(*config.SDKConfiguration) error

// WithBaseURL sets the base URL for the SDK
func WithBaseURL(baseURL string) SdkOption {
	return func(cfg *config.SDKConfiguration) error {
		return cfg.SetServerURL(baseURL)
	}
}

// WithHTTPClient sets a custom HTTP client for the SDK
func WithHTTPClient(client *http.Client) SdkOption {
	return func(cfg *config.SDKConfiguration) error {
		cfg.SetClient(client)
		return nil
	}
}