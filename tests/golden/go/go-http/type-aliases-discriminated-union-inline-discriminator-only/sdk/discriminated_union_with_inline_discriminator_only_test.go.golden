// Code generated by openapi-nexus. DO NOT EDIT.
// Discriminated Union with Inline Discriminator Only Test
//
// Test fixture for discriminated union where variants are inline objects
// with ONLY a discriminator property (no additional fields).
// 
// This specifically tests the fix for the bug where inline objects with
// only a discriminator property (like { kind: "UNSPECIFIED" }) were
// generating generic "Kind.ts" files that conflicted across different
// discriminated unions.
// 
// After the fix, these should generate properly prefixed names like
// "EventKindUnspecified" instead of generic "Kind".
// 
//
// Generated from OpenAPI doc version 1.0.0

package sdk

import (
	"net/http"

	"example.com/sdk/internal/config"
	"example.com/sdk/internal/hooks"
	"example.com/sdk/apis"
)

// DiscriminatedUnionWithInlineDiscriminatorOnlyTestSDK is the main SDK struct
type DiscriminatedUnionWithInlineDiscriminatorOnlyTestSDK struct {
	sdkConfiguration config.SDKConfiguration
	hooks            *hooks.Hooks
	default_ *apis.Default
}

// Ensure DiscriminatedUnionWithInlineDiscriminatorOnlyTestSDK implements hooks.SDK interface
var _ hooks.SDK = (*DiscriminatedUnionWithInlineDiscriminatorOnlyTestSDK)(nil)

// New creates a new instance of DiscriminatedUnionWithInlineDiscriminatorOnlyTestSDK
func New(opts ...SdkOption) (*DiscriminatedUnionWithInlineDiscriminatorOnlyTestSDK, error) {
	sdkConfig := config.NewSDKConfiguration()
	h := hooks.New()

	// Apply options
	for _, opt := range opts {
		if err := opt(&sdkConfig); err != nil {
			return nil, err
		}
	}

	// Initialize hooks
	sdkConfig = h.SDKInit(sdkConfig)

	sdk := &DiscriminatedUnionWithInlineDiscriminatorOnlyTestSDK{
		sdkConfiguration: sdkConfig,
		hooks:            h,
	}

	// Initialize sub-clients
	sdk.default_ = apis.NewDefault(sdk, sdkConfig, h)

	return sdk, nil
}

// SdkOption is a function that configures the SDK
type SdkOption func(*config.SDKConfiguration) error

// WithBaseURL sets the base URL for the SDK
func WithBaseURL(baseURL string) SdkOption {
	return func(cfg *config.SDKConfiguration) error {
		return cfg.SetServerURL(baseURL)
	}
}

// WithHTTPClient sets a custom HTTP client for the SDK
func WithHTTPClient(client *http.Client) SdkOption {
	return func(cfg *config.SDKConfiguration) error {
		cfg.SetClient(client)
		return nil
	}
}