// Code generated by openapi-nexus. DO NOT EDIT.
// Duplicate Parameter Names Test API
//
// Test API with duplicate parameter names across different locations
//
// Generated from OpenAPI doc version 1.0.0

package operations

import (
	"context"
	"fmt"
	"net/http"
	"net/url"

	"example.com/sdk/internal/config"
	"example.com/sdk/internal/hooks"
	"example.com/sdk/internal/utils"
	"example.com/sdk/models/components"
	"example.com/sdk/models/operations"
)

// Items - items operations
type Items struct {
	rootSDK          *DuplicateParameterNamesTestApiSDK
	sdkConfiguration config.SDKConfiguration
	hooks            *hooks.Hooks
}

// NewItems creates a new Items client
func NewItems(rootSDK *DuplicateParameterNamesTestApiSDK, sdkConfig config.SDKConfiguration, hooks *hooks.Hooks) *Items {
	return &Items{
		rootSDK:          rootSDK,
		sdkConfiguration: sdkConfig,
		hooks:            hooks,
	}
}

// CreateItemWithBodyConflict - POST /items/{itemId}
func (s *Items) CreateItemWithBodyConflict(
	ctx context.Context,
	pathItemId string,
	queryBody string,
	request components.CreateItemWithBodyConflictRequest,
	opts ...operations.Option,
) (*operations.CreateItemWithBodyConflictResponse, error) {
	o := operations.Options{}
	supportedOptions := []string{
		operations.SupportedOptionRetries,
		operations.SupportedOptionTimeout,
	}

	for _, opt := range opts {
		if err := opt(&o, supportedOptions...); err != nil {
			return nil, fmt.Errorf("error applying option: %w", err)
		}
	}

	var baseURL string
	if o.ServerURL == nil {
		baseURL, _ = s.sdkConfiguration.GetServerDetails()
	} else {
		baseURL = *o.ServerURL
	}
	opURL, err := url.JoinPath(baseURL, "/items/{itemId}")
	if err != nil {
		return nil, fmt.Errorf("error generating URL: %w", err)
	}

	// Build path parameters
	pathParams := map[string]string{
		"itemId": fmt.Sprintf("%v", pathItemId),
	}
	opURL = utils.ReplaceParameters(opURL, pathParams)

	hookCtx := hooks.HookContext{
		SDK:              s.rootSDK,
		SDKConfiguration: s.sdkConfiguration,
		BaseURL:          baseURL,
		Context:          ctx,
		OperationID:      "create_item_with_body_conflict",
		OAuth2Scopes:     nil,
		SecuritySource:   nil,
	}

	bodyReader, reqContentType, err := utils.SerializeRequestBody(ctx, request, false, false, "Request", "json", `request:"mediaType=application/json"`)
	if err != nil {
		return nil, err
	}

	timeout := o.Timeout
	if timeout == nil {
		timeout = s.sdkConfiguration.Timeout
	}

	if timeout != nil {
		var cancel context.CancelFunc
		ctx, cancel = context.WithTimeout(ctx, *timeout)
		defer cancel()
	}

	req, err := http.NewRequestWithContext(ctx, "POST", opURL, bodyReader)
	if err != nil {
		return nil, fmt.Errorf("error creating request: %w", err)
	}
	req.Header.Set("Accept", "application/json")
	req.Header.Set("User-Agent", s.sdkConfiguration.UserAgent)
	if reqContentType != "" {
		req.Header.Set("Content-Type", reqContentType)
	}

	// Build query parameters
	queryParams := struct {
		Body string `queryParam:"name=body,style=form,explode=true"`
	}{
		Body: queryBody,
	}
	if err := utils.PopulateQueryParams(ctx, req, queryParams, nil); err != nil {
		return nil, err
	}


	for k, v := range o.SetHeaders {
		req.Header.Set(k, v)
	}

	globalRetryConfig := s.sdkConfiguration.RetryConfig
	retryConfig := o.Retries
	if retryConfig == nil {
		retryConfig = globalRetryConfig
	}

	httpRes, err := utils.Retry(ctx, utils.Retries{
		Config:      retryConfig,
		StatusCodes: []string{"5XX"},
	}, func() (*http.Response, error) {
		if req.Body != nil {
			copyBody, err := req.GetBody()
			if err != nil {
				return nil, err
			}
			req.Body = copyBody
		}

		req, err = s.hooks.BeforeRequest(hooks.BeforeRequestContext{HookContext: hookCtx}, req)
		if err != nil {
			return nil, err
		}

		httpRes, err := s.sdkConfiguration.Client.Do(req)
		if err != nil || httpRes == nil {
			if err != nil {
				hookCtx := hooks.AfterErrorContext{HookContext: hookCtx}
				httpRes, err = s.hooks.AfterError(hookCtx, httpRes, err)
			}
			return httpRes, err
		}

		httpRes, err = s.hooks.AfterSuccess(hooks.AfterSuccessContext{HookContext: hookCtx}, httpRes)
		if err != nil {
			return httpRes, err
		}

		return httpRes, nil
	})
	if err != nil {
		return nil, err
	}

	defer httpRes.Body.Close()

	res := &operations.CreateItemWithBodyConflictResponse{
		HTTPMeta: components.HTTPMetadata{
			Response: httpRes,
		},
	}

	switch {
	case utils.MatchStatusCodes([]string{"200"}, httpRes.StatusCode):
		// TODO: Parse response body based on content type
	default:
		rawBody, err := utils.ConsumeRawBody(httpRes)
		if err != nil {
			return nil, err
		}
		httpRes.Body.Close()
		return nil, fmt.Errorf("error: status code %d, body: %s", httpRes.StatusCode, string(rawBody))
	}

	return res, nil
}

