# Code generated by openapi-nexus. DO NOT EDIT.
#
# Multiple Discriminated Unions Test — 1.0.0
# Test fixture with multiple discriminated unions to verify Kind types
# are properly namespaced and don't conflict.
# This reproduces the original bug where both unions would generate
# the same Kind.ts file causing conflicts.

from __future__ import annotations

from ..models.container_kind import ContainerKind
from ..models.volume_kind import VolumeKind
from ..runtime.client import Client
from ..runtime.errors import ApiError

class DefaultApi:
    def __init__(self, client: Client) -> None:
        self._client = client

    def create_container(self, *, body: ContainerKind) -> ContainerKind:
        """Create container with ContainerKind discriminated union."""
        path = "/container"
        response = self._client.request("POST", path, json=body)
        if response.status_code >= 400:
            raise ApiError(response.status_code, response.reason_phrase, response.content)
        return response.json()  # type: ignore[return-value]

    def create_volume(self, *, body: VolumeKind) -> VolumeKind:
        """Create volume with VolumeKind discriminated union."""
        path = "/volume"
        response = self._client.request("POST", path, json=body)
        if response.status_code >= 400:
            raise ApiError(response.status_code, response.reason_phrase, response.content)
        return response.json()  # type: ignore[return-value]
