# Code generated by openapi-nexus. DO NOT EDIT.
#
# Enum Representation API — 1.0.0
# This API demonstrates all 4 kinds of enum representation types: Externally Tagged, Internally Tagged, Adjacently Tagged, and Untagged

from __future__ import annotations

from ..models.adjacently_tagged_enum import AdjacentlyTaggedEnum
from ..models.externally_tagged_enum import ExternallyTaggedEnum
from ..models.internally_tagged_enum import InternallyTaggedEnum
from ..models.mixed_enum import MixedEnum
from ..models.untagged_enum import UntaggedEnum
from ..runtime.client import Client
from ..runtime.errors import ApiError

class EnumReprApi:
    def __init__(self, client: Client) -> None:
        self._client = client

    def handle_adjacently_tagged(self, *, body: AdjacentlyTaggedEnum) -> AdjacentlyTaggedEnum:
        """Handle adjacently tagged enum."""
        path = "/adjacently-tagged"
        headers: dict[str, str] = {}
        headers["Content-Type"] = "application/json"
        response = self._client.request("POST", path, json=body, headers=headers)
        if response.status_code >= 400:
            raise ApiError(response.status_code, response.reason_phrase, response.content)
        return response.json()  # type: ignore[return-value]

    def handle_externally_tagged(self, *, body: ExternallyTaggedEnum) -> ExternallyTaggedEnum:
        """Handle externally tagged enum."""
        path = "/externally-tagged"
        headers: dict[str, str] = {}
        headers["Content-Type"] = "application/json"
        response = self._client.request("POST", path, json=body, headers=headers)
        if response.status_code >= 400:
            raise ApiError(response.status_code, response.reason_phrase, response.content)
        return response.json()  # type: ignore[return-value]

    def handle_internally_tagged(self, *, body: InternallyTaggedEnum) -> InternallyTaggedEnum:
        """Handle internally tagged enum."""
        path = "/internally-tagged"
        headers: dict[str, str] = {}
        headers["Content-Type"] = "application/json"
        response = self._client.request("POST", path, json=body, headers=headers)
        if response.status_code >= 400:
            raise ApiError(response.status_code, response.reason_phrase, response.content)
        return response.json()  # type: ignore[return-value]

    def handle_mixed(self, *, body: MixedEnum) -> MixedEnum:
        """Handle mixed enum (unit variants SimpleA, SimpleB and tuple variants VariantA(VariantA), VariantB(VariantB))."""
        path = "/mixed"
        headers: dict[str, str] = {}
        headers["Content-Type"] = "application/json"
        response = self._client.request("POST", path, json=body, headers=headers)
        if response.status_code >= 400:
            raise ApiError(response.status_code, response.reason_phrase, response.content)
        return response.json()  # type: ignore[return-value]

    def handle_untagged(self, *, body: UntaggedEnum) -> UntaggedEnum:
        """Handle untagged enum."""
        path = "/untagged"
        headers: dict[str, str] = {}
        headers["Content-Type"] = "application/json"
        response = self._client.request("POST", path, json=body, headers=headers)
        if response.status_code >= 400:
            raise ApiError(response.status_code, response.reason_phrase, response.content)
        return response.json()  # type: ignore[return-value]
