[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "optional_request_bodies"
version = "1.0.0"
description = "Covers optional non-multipart request bodies."
requires-python = ">=3.12"
dependencies = ["httpx>=0.27"]
