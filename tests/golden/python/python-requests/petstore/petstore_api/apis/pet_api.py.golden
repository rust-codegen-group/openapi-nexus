# Code generated by openapi-nexus. DO NOT EDIT.
#
# Petstore API — 1.0.0
# This is a sample Pet Store Server based on the OpenAPI 3.1 specification

from __future__ import annotations

from ..models.pet import Pet
from ..models.upload_response import UploadResponse
from ..runtime.client import Client
from ..runtime.errors import ApiError

class PetApi:
    def __init__(self, client: Client) -> None:
        self._client = client

    def update_pet(self, *, body: Pet) -> Pet:
        """Update an existing pet."""
        path = "/pet"
        response = self._client.request("PUT", path, json=body.to_dict())
        if response.status_code >= 400:
            raise ApiError(response.status_code, response.reason, response.content)
        return Pet.from_dict(response.json())

    def add_pet(self, *, body: Pet) -> Pet:
        """Add a new pet to the store."""
        path = "/pet"
        response = self._client.request("POST", path, json=body.to_dict())
        if response.status_code >= 400:
            raise ApiError(response.status_code, response.reason, response.content)
        return Pet.from_dict(response.json())

    def find_pets_by_status(self, *, status: str) -> list[Pet]:
        """Find pets by status."""
        path = "/pet/findByStatus"
        params: dict[str, str] = {}
        params["status"] = str(status)
        response = self._client.request("GET", path, params=params)
        if response.status_code >= 400:
            raise ApiError(response.status_code, response.reason, response.content)
        return [Pet.from_dict(item) for item in response.json()]

    def find_pets_by_tags(self, *, tags: list[str]) -> list[Pet]:
        """Find pets by tags."""
        path = "/pet/findByTags"
        params: dict[str, str] = {}
        params["tags"] = ",".join(str(v) for v in tags)
        response = self._client.request("GET", path, params=params)
        if response.status_code >= 400:
            raise ApiError(response.status_code, response.reason, response.content)
        return [Pet.from_dict(item) for item in response.json()]

    def get_pet_by_id(self, pet_id: int) -> Pet:
        """Find pet by ID."""
        path = f"/pet/{pet_id}"
        response = self._client.request("GET", path)
        if response.status_code >= 400:
            raise ApiError(response.status_code, response.reason, response.content)
        return Pet.from_dict(response.json())

    def update_pet_with_form(self, pet_id: int, *, name: str | None = None, status: str | None = None) -> Pet:
        """Update a pet in the store with form data."""
        path = f"/pet/{pet_id}"
        params: dict[str, str] = {}
        if name is not None:
            params["name"] = str(name)
        if status is not None:
            params["status"] = str(status)
        response = self._client.request("POST", path, params=params)
        if response.status_code >= 400:
            raise ApiError(response.status_code, response.reason, response.content)
        return Pet.from_dict(response.json())

    def delete_pet(self, pet_id: int) -> None:
        """Delete a pet."""
        path = f"/pet/{pet_id}"
        response = self._client.request("DELETE", path)
        if response.status_code >= 400:
            raise ApiError(response.status_code, response.reason, response.content)
        return None

    def upload_file(self, pet_id: int, *, additional_metadata: str | None = None) -> UploadResponse:
        """Upload an image."""
        path = f"/pet/{pet_id}/uploadImage"
        params: dict[str, str] = {}
        if additional_metadata is not None:
            params["additionalMetadata"] = str(additional_metadata)
        response = self._client.request("POST", path, params=params)
        if response.status_code >= 400:
            raise ApiError(response.status_code, response.reason, response.content)
        return UploadResponse.from_dict(response.json())
