[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "simple_type_alias_test"
version = "1.0.0"
description = "Test fixture for simple type aliases (not unions or intersections)"
requires-python = ">=3.12"
dependencies = ["requests>=2.32"]
