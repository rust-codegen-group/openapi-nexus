[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "union_type_with_mixed_members_test"
version = "1.0.0"
description = "Test fixture for union types with both interfaces and primitives"
requires-python = ">=3.12"
dependencies = ["requests>=2.32"]
