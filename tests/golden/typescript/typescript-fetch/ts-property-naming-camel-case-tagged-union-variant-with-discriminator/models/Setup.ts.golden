/**
 * @generated by openapi-nexus. Do not edit.
 *
 * Discriminated Union - Variant Includes Discriminator Field — 1.0.0
 * Test fixture where variant schemas include the discriminator property in their own
 * properties. This is the pattern that triggers TS2783 if the fromJSON/toJSON functions
 * put the discriminator literal BEFORE the spread (since the spread would overwrite it).
 * The fix is to put the spread first, then the discriminator literal last so it wins.
 */
import type { SetupAutomatic, SetupAutomatic$Wire } from './SetupAutomatic';
import { setupAutomaticFromJSON, setupAutomaticToJSON } from './SetupAutomatic';
import type { SetupManual, SetupManual$Wire } from './SetupManual';
import { setupManualFromJSON, setupManualToJSON } from './SetupManual';
import type { SetupUnspecified, SetupUnspecified$Wire } from './SetupUnspecified';
import { setupUnspecifiedFromJSON, setupUnspecifiedToJSON } from './SetupUnspecified';

export type Setup$Wire = ({ kind: 'SETUP_UNSPECIFIED' } & SetupUnspecified$Wire) | ({ kind: 'SETUP_AUTOMATIC' } & SetupAutomatic$Wire) | ({ kind: 'SETUP_MANUAL' } & SetupManual$Wire);

export type Setup = ({ kind: 'SETUP_UNSPECIFIED' } & SetupUnspecified) | ({ kind: 'SETUP_AUTOMATIC' } & SetupAutomatic) | ({ kind: 'SETUP_MANUAL' } & SetupManual);

export function setupFromJSON(json: Setup$Wire): Setup {
  switch (json.kind) {
    case 'SETUP_UNSPECIFIED': return { ...setupUnspecifiedFromJSON(json), kind: 'SETUP_UNSPECIFIED' };
    case 'SETUP_AUTOMATIC': return { ...setupAutomaticFromJSON(json), kind: 'SETUP_AUTOMATIC' };
    case 'SETUP_MANUAL': return { ...setupManualFromJSON(json), kind: 'SETUP_MANUAL' };
  }
}

export function setupToJSON(value: Setup): Setup$Wire {
  switch (value.kind) {
    case 'SETUP_UNSPECIFIED': return { ...setupUnspecifiedToJSON(value), kind: 'SETUP_UNSPECIFIED' } as Setup$Wire;
    case 'SETUP_AUTOMATIC': return { ...setupAutomaticToJSON(value), kind: 'SETUP_AUTOMATIC' } as Setup$Wire;
    case 'SETUP_MANUAL': return { ...setupManualToJSON(value), kind: 'SETUP_MANUAL' } as Setup$Wire;
  }
}
