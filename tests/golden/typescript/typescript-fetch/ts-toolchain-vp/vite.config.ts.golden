import { defineConfig } from 'vite-plus';

export default defineConfig({
  lint: {
    options: {
      typeAware: true,
      typeCheck: true,
    },
    ignorePatterns: ['node_modules', 'dist'],
  },
  pack: {
    dts: true,
    format: ['esm'],
  },
});
