import { defineConfig } from 'vite-plus';

export default defineConfig({
  lint: {
    options: {
      typeAware: true,
      typeCheck: true,
    },
    ignorePatterns: ['node_modules', 'dist'],
  },
  pack: {
    entry: ['index.ts'],
    dts: true,
    format: ['esm'],
  },
});
