// @generated
// Code generated by openapi-nexus. DO NOT EDIT.
//
// Server Path Prefix Test — 1.0.0
// Test fixture for server URL path prefix stripping in operation paths.

use crate::runtime::client::Client;
use crate::runtime::error::Error;

/// API operations under the "default" tag.
pub struct DefaultApi<'a> {
    client: &'a Client,
}

impl<'a> DefaultApi<'a> {
    /// Create a new `DefaultApi` bound to the given client.
    pub fn new(client: &'a Client) -> Self {
        Self {
            client,
        }
    }

    /// Health check
    pub fn health_check(
        &self,
    ) -> Result<HealthCheckResponse, Error> {
        let path = "/health".to_string();
        let req = self.client.get(&path);
        let resp = req.call()?;
        let status_code = resp.status().as_u16();
        Ok(HealthCheckResponse { status_code })
    }

    /// List users
    pub fn list_users(
        &self,
    ) -> Result<ListUsersResponse, Error> {
        let path = "/users".to_string();
        let req = self.client.get(&path);
        let resp = req.call()?;
        let status_code = resp.status().as_u16();
        Ok(ListUsersResponse { status_code })
    }

    /// Get user
    pub fn get_user(
        &self,
        user_id: &str,
    ) -> Result<GetUserResponse, Error> {
        let mut path = "/users/{user_id}".to_string();
        path = path.replace("{user_id}", &user_id.to_string());
        let req = self.client.get(&path);
        let resp = req.call()?;
        let status_code = resp.status().as_u16();
        Ok(GetUserResponse { status_code })
    }
}

/// Response from `health_check`.
#[derive(Debug)]
pub struct HealthCheckResponse {
    pub status_code: u16,
}

/// Response from `list_users`.
#[derive(Debug)]
pub struct ListUsersResponse {
    pub status_code: u16,
}

/// Response from `get_user`.
#[derive(Debug)]
pub struct GetUserResponse {
    pub status_code: u16,
}
