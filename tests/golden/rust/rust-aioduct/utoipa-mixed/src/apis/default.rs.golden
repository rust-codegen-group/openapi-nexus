// @generated
// Code generated by openapi-nexus. DO NOT EDIT.
//
// Utoipa Mixed Types Test — 1.0.0
// Test fixture covering all schema kinds with utoipa enabled

use crate::runtime::client::Client;
use crate::runtime::error::Error;

/// API operations under the "default" tag.
pub struct DefaultApi<'a, R: aioduct::Runtime> {
    client: &'a Client<R>,
}

impl<'a, R: aioduct::Runtime> DefaultApi<'a, R> {
    /// Create a new `DefaultApi` bound to the given client.
    pub fn new(client: &'a Client<R>) -> Self {
        Self {
            client,
        }
    }

    /// Create feed
    pub async fn create_feed(
        &self,
        body: &crate::models::FeedSource,
    ) -> Result<CreateFeedResponse, Error> {
        let path = "/feeds".to_string();
        let mut req = self.client.post(&path)?;
        req = req.json(&body)?;
        let resp = req.send().await?;
        let status_code = resp.status().as_u16();
        let body_bytes = resp.bytes().await?;
        let mut result = CreateFeedResponse { status_code, data: None };
        match status_code {
            200 => {
                result.data = Some(serde_json::from_slice(&body_bytes).map_err(Error::Deserialize)?);
            }
            _ => {}
        }
        Ok(result)
    }

    /// Create payment
    pub async fn create_payment(
        &self,
        body: &crate::models::PaymentMethod,
    ) -> Result<CreatePaymentResponse, Error> {
        let path = "/payments".to_string();
        let mut req = self.client.post(&path)?;
        req = req.json(&body)?;
        let resp = req.send().await?;
        let status_code = resp.status().as_u16();
        let body_bytes = resp.bytes().await?;
        let mut result = CreatePaymentResponse { status_code, data: None };
        match status_code {
            200 => {
                result.data = Some(serde_json::from_slice(&body_bytes).map_err(Error::Deserialize)?);
            }
            _ => {}
        }
        Ok(result)
    }

    /// List pets
    pub async fn list_pets(
        &self,
        status: &Option<crate::models::PetStatus>,
    ) -> Result<ListPetsResponse, Error> {
        let mut path = "/pets".to_string();
        let mut query_parts: Vec<(&str, String)> = Vec::new();
        if let Some(v) = &status {
            query_parts.push(("status", v.to_string()));
        }
        if !query_parts.is_empty() {
            let qs = url::form_urlencoded::Serializer::new(String::new()).extend_pairs(query_parts.iter().map(|(k, v)| (*k, v.as_str()))).finish();
            path = format!("{}?{}", path, qs);
        }
        let req = self.client.get(&path)?;
        let resp = req.send().await?;
        let status_code = resp.status().as_u16();
        let body_bytes = resp.bytes().await?;
        let mut result = ListPetsResponse { status_code, data: None };
        match status_code {
            200 => {
                result.data = Some(serde_json::from_slice(&body_bytes).map_err(Error::Deserialize)?);
            }
            _ => {}
        }
        Ok(result)
    }

    /// Create pet
    pub async fn create_pet(
        &self,
        body: &crate::models::Pet,
    ) -> Result<CreatePetResponse, Error> {
        let path = "/pets".to_string();
        let mut req = self.client.post(&path)?;
        req = req.json(&body)?;
        let resp = req.send().await?;
        let status_code = resp.status().as_u16();
        let body_bytes = resp.bytes().await?;
        let mut result = CreatePetResponse { status_code, created: None };
        match status_code {
            201 => {
                result.created = Some(serde_json::from_slice(&body_bytes).map_err(Error::Deserialize)?);
            }
            _ => {}
        }
        Ok(result)
    }
}

/// Response from `create_feed`.
#[derive(Debug)]
pub struct CreateFeedResponse {
    pub status_code: u16,
    pub data: Option<crate::models::FeedSource>,
}

/// Response from `create_payment`.
#[derive(Debug)]
pub struct CreatePaymentResponse {
    pub status_code: u16,
    pub data: Option<crate::models::PaymentMethod>,
}

/// Response from `list_pets`.
#[derive(Debug)]
pub struct ListPetsResponse {
    pub status_code: u16,
    pub data: Option<Vec<crate::models::Pet>>,
}

/// Response from `create_pet`.
#[derive(Debug)]
pub struct CreatePetResponse {
    pub status_code: u16,
    pub created: Option<crate::models::Pet>,
}
