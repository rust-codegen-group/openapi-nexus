// @generated
// Code generated by openapi-nexus. DO NOT EDIT.
//
// Request Body Content Types — 1.0.0
// Covers non-JSON request body media types to pin Content-Type emission.

use crate::runtime::client::Client;
use crate::runtime::error::Error;

/// API operations under the "default" tag.
pub struct DefaultApi<'a, R: aioduct::Runtime> {
    client: &'a Client<R>,
}

impl<'a, R: aioduct::Runtime> DefaultApi<'a, R> {
    /// Create a new `DefaultApi` bound to the given client.
    pub fn new(client: &'a Client<R>) -> Self {
        Self {
            client,
        }
    }

    /// POST /binary
    pub async fn post_binary(
        &self,
        body: &Vec<u8>,
    ) -> Result<PostBinaryResponse, Error> {
        let path = "/binary".to_string();
        let mut req = self.client.post(&path)?;
        req = req.header_str("Content-Type", "application/octet-stream")?;
        req = req.body(body.clone());
        let resp = req.send().await?;
        let status_code = resp.status().as_u16();
        Ok(PostBinaryResponse { status_code })
    }

    /// POST /form
    pub async fn post_form(
        &self,
        body: &crate::models::Payload,
    ) -> Result<PostFormResponse, Error> {
        let path = "/form".to_string();
        let mut req = self.client.post(&path)?;
        req = req.form_serde(&body)?;
        let resp = req.send().await?;
        let status_code = resp.status().as_u16();
        Ok(PostFormResponse { status_code })
    }

    /// POST /json
    pub async fn post_json(
        &self,
        body: &crate::models::Payload,
    ) -> Result<PostJsonResponse, Error> {
        let path = "/json".to_string();
        let mut req = self.client.post(&path)?;
        req = req.json(&body)?;
        let resp = req.send().await?;
        let status_code = resp.status().as_u16();
        Ok(PostJsonResponse { status_code })
    }

    /// POST /json-or-xml
    ///
    /// Multiple media types declared — JSON should be preferred.
    pub async fn post_json_or_xml(
        &self,
        body: &crate::models::Payload,
    ) -> Result<PostJsonOrXmlResponse, Error> {
        let path = "/json-or-xml".to_string();
        let mut req = self.client.post(&path)?;
        req = req.json(&body)?;
        let resp = req.send().await?;
        let status_code = resp.status().as_u16();
        Ok(PostJsonOrXmlResponse { status_code })
    }

    /// PATCH /merge-patch
    pub async fn patch_merge_json(
        &self,
        body: &crate::models::Payload,
    ) -> Result<PatchMergeJsonResponse, Error> {
        let path = "/merge-patch".to_string();
        let mut req = self.client.patch(&path)?;
        req = req.json(&body)?;
        let resp = req.send().await?;
        let status_code = resp.status().as_u16();
        Ok(PatchMergeJsonResponse { status_code })
    }

    /// POST /text
    pub async fn post_text(
        &self,
        body: &String,
    ) -> Result<PostTextResponse, Error> {
        let path = "/text".to_string();
        let mut req = self.client.post(&path)?;
        req = req.header_str("Content-Type", "text/plain")?;
        req = req.body(body.clone().into_bytes());
        let resp = req.send().await?;
        let status_code = resp.status().as_u16();
        Ok(PostTextResponse { status_code })
    }

    /// POST /xml
    pub async fn post_xml(
        &self,
        body: &crate::models::Payload,
    ) -> Result<PostXmlResponse, Error> {
        let path = "/xml".to_string();
        let mut req = self.client.post(&path)?;
        req = req.header_str("Content-Type", "application/xml")?;
        let body_xml = serde_xml_rs::to_string(body)?;
        req = req.body(body_xml.into_bytes());
        let resp = req.send().await?;
        let status_code = resp.status().as_u16();
        Ok(PostXmlResponse { status_code })
    }

    /// GET /xml-response
    pub async fn get_xml_response(
        &self,
    ) -> Result<GetXmlResponseResponse, Error> {
        let path = "/xml-response".to_string();
        let req = self.client.get(&path)?;
        let resp = req.send().await?;
        let status_code = resp.status().as_u16();
        let body_bytes = resp.bytes().await?;
        let mut result = GetXmlResponseResponse { status_code, data: None };
        match status_code {
            200 => {
                result.data = Some(serde_xml_rs::from_reader(std::io::Cursor::new(&body_bytes)).map_err(Error::Xml)?);
            }
            _ => {}
        }
        Ok(result)
    }
}

/// Response from `post_binary`.
#[derive(Debug)]
pub struct PostBinaryResponse {
    pub status_code: u16,
}

/// Response from `post_form`.
#[derive(Debug)]
pub struct PostFormResponse {
    pub status_code: u16,
}

/// Response from `post_json`.
#[derive(Debug)]
pub struct PostJsonResponse {
    pub status_code: u16,
}

/// Response from `post_json_or_xml`.
#[derive(Debug)]
pub struct PostJsonOrXmlResponse {
    pub status_code: u16,
}

/// Response from `patch_merge_json`.
#[derive(Debug)]
pub struct PatchMergeJsonResponse {
    pub status_code: u16,
}

/// Response from `post_text`.
#[derive(Debug)]
pub struct PostTextResponse {
    pub status_code: u16,
}

/// Response from `post_xml`.
#[derive(Debug)]
pub struct PostXmlResponse {
    pub status_code: u16,
}

/// Response from `get_xml_response`.
#[derive(Debug)]
pub struct GetXmlResponseResponse {
    pub status_code: u16,
    pub data: Option<crate::models::Payload>,
}
